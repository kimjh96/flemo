---
title: Router & Route
description: 라우트 매칭, 등록, 기본 옵션
---

`<Router>`는 루트 컨테이너예요. URL에 맞는 `<Route>`를 골라 렌더해요.

## 구성 요소

```tsx
<Router>
  <Route path="/" element={<Home />} />
  <Route path="/posts/:slug" element={<Post />} />
</Router>
```

| 요소       | 역할                                          |
| ---------- | --------------------------------------------- |
| `<Router>` | history·트랜지션·데코레이터를 설정해요        |
| `<Route>`  | `path`(또는 여러 path)를 `element`에 매핑해요 |

## 경로 패턴

경로 매칭은 [path-to-regexp](https://github.com/pillarjs/path-to-regexp) v8을 그대로 써요.

```ts
"/"; // 정확 매칭
"/posts/:slug"; // 단일 파라미터
"/users/:id/posts/:p"; // 다중 파라미터
"/files/*splat"; // 와일드카드 (파라미터 앞 * 표시에 주의)
```

여러 경로를 한 컴포넌트에 매핑하려면 배열로 넘겨요.

```tsx
<Route path={["/", "/home"]} element={<Home />} />
```

## 타입 안전한 라우트

`RegisterRoute`를 augment 하면 `navigate.push`, `useParams` 등이 모두 이를 기준으로 타입 검증돼요. 한 파일에
모아서 선언하든, 각 페이지 파일에서 자기가 쓰는 라우트만 선언하든 — TypeScript가 모든 선언을
합쳐서 보기 때문에 어느 쪽이든 동작해요. 보통 `<Router>`를 마운트하는 파일 하단에 같이 두면
편해요.

```ts title="App.tsx"
declare module "@flemo/react" {
  interface RegisterRoute {
    "/": undefined;
    "/posts/:slug": { slug: string };
    "/users/:id": { id: string };
  }
}
```

파라미터가 없는 경로는 `undefined`, 있는 경로는 그대로 shape를 적어요.

```tsx
navigate.push("/posts/:slug", { slug: "hello" }); // ✅
navigate.push("/posts/:slug", { id: "1" }); // ❌ 타입 에러
navigate.push("/unknown"); // ❌ 타입 에러
```

## Router 옵션

```tsx
<Router initPath="/" defaultTransitionName="cupertino" transitions={[]} decorators={[]}>
  {/* routes */}
</Router>
```

| Prop                    | 기본값        | 설명                                                    |
| ----------------------- | ------------- | ------------------------------------------------------- |
| `initPath`              | `"/"`         | SSR에서 `window.location`이 없을 때 쓰는 경로           |
| `defaultTransitionName` | `"cupertino"` | `navigate.push`가 전환을 지정하지 않았을 때의 기본 전환 |
| `transitions`           | `[]`          | 등록할 커스텀 전환 ([Transitions](./transitions) 참고)  |
| `decorators`            | `[]`          | 등록할 커스텀 데코레이터 (예: 오버레이)                 |

## SSR

flemo는 **클라이언트 사이드 SPA 라우터**예요 — `window.history`를 직접 구동하고, 마운트되면
내비게이션을 넘겨받아요. `<Router>`는 첫 렌더를 서버에서 그릴 수도 있는데, 서버에는
`window.location`이 없으니 첫 화면 경로를 `initPath`로 알려줘야 해요.

- **서버**: `initPath`에 해당하는 화면을 렌더해요. 지정하지 않으면 `"/"`예요.
- **클라이언트**: 마운트되면 `window.location.pathname`을 읽어 그대로 이어받아요.

요청 경로를 `initPath`로 넘기면 hydration mismatch 없이 첫 화면이 맞아요. React를 문자열로
렌더한 뒤 라우팅을 flemo에 맡기는 어떤 환경에서도 동작해요:

```tsx
// 서버에서 들어온 요청 경로를 initPath로 넘겨요.
<Router initPath={requestPath}>{/* routes */}</Router>
```

서버 렌더가 필요 없는 SPA(Vite 등)라면 `initPath`는 신경 쓰지 않아도 돼요. 클라이언트에서 바로
`window.location.pathname`을 읽어요.

<Callout type="warn">
  flemo는 클라이언트 사이드 history를 직접 소유하기 때문에(`window.history`에 push하고 `popstate`를
  청취해요), **라우팅을 직접 관리하는 호스트 프레임워크와는 공존하지 못해요** — 특히 **Next.js App
  Router**가 그래요. 두 라우터가 내비게이션과 URL을 두고 충돌해서, flemo가 Next.js 앱의 페이지
  라우터 역할을 할 수는 없어요. 순수 SPA에서 쓰거나, 호스트 프레임워크와 라우팅을 공유하지 않는
  독립적인 클라이언트 전용 섬(island)으로 마운트하세요.
</Callout>

## Router 밖에서 상태 읽기

Router 트리 안에서는 — `<Route>` element와 그 아래 컴포넌트들 — 내비게이션이 그냥 동작해요.
`useNavigate`, `useScreen`, 그리고 아래 스토어 훅들 모두 현재 Router의 상태를 읽어요. 따로 설정할
게 없어요.

스토어는 **요청 단위(request-scoped)**예요. `<Router>`가 마운트마다 묶음을 하나 만들어 context로
공유하기 때문에, 동시에 들어오는 SSR 요청끼리 서로 격리돼요. 단점은, Router *밖*에 렌더된
컴포넌트 — devtools 패널이나 화면 프레임 옆의 인스펙터 — 는 읽을 context가 없어서 스토어 훅이
에러를 던진다는 거예요.

Router와 그 바깥 형제를 `<RouterScopeProvider>`로 함께 감싸면 하나의 묶음을 공유해요. Router는
자기 묶음을 새로 만들지 않고 이 호스팅된 묶음을 채택하기 때문에, 형제가 같은 상태를 읽고 — 구동할
수도 — 있어요.

```tsx
import { Router, Route, RouterScopeProvider, useHistoryStore, useNavigate } from "@flemo/react";

function Inspector() {
  // 셀렉터 읽기 — zustand 훅처럼 스택이 바뀌면 리렌더돼요.
  const depth = useHistoryStore((state) => state.histories.length);
  const navigate = useNavigate(); // 옆에 렌더된 Router를 구동해요

  return (
    <aside>
      <p>스택 깊이: {depth}</p>
      <button onClick={() => navigate.pop()}>뒤로</button>
    </aside>
  );
}

<RouterScopeProvider>
  <Router>
    <Route path="/" element={<Home />} />
    <Route path="/posts/:slug" element={<Post />} />
  </Router>
  <Inspector /> {/* Router 밖이지만 같은 스토어 */}
</RouterScopeProvider>;
```

모든 스토어 훅은 현재 Router의 요청 단위 스토어를 읽어요.

| 훅                       | 읽는 것                                                |
| ------------------------ | ------------------------------------------------------ |
| `useHistoryStore(fn)`    | history 스택(`index`, `histories`)과 그 mutator        |
| `useNavigateStore(fn)`   | 내비게이션 상태(`IDLE` / `PUSHING` / `POPPING` / …)    |
| `useTransitionStore(fn)` | 기본 전환 이름                                         |
| `useScreenStore(fn)`     | 드래그 / replace 상태와 shared-bar 레지스트리          |
| `useStores()`            | 명령형 `getState()` / `subscribe`용 네 스토어 API 전체 |

셀렉터 훅 네 개는 셀렉터를 받아 값이 바뀌면 리렌더돼요. `useStores()`는 React 렌더 밖에서 읽는
이벤트 핸들러·이펙트를 위해 원본 스토어 API를 그대로 돌려줘요.

<Callout>
  `<RouterScopeProvider>`는 **선택 사항**이에요 — `<Router>` 밖에서 상태를 읽거나 구동해야 할 때만
  쓰세요. 보통 앱은 `<Router>`만 렌더하면 되고 이건 필요 없어요. [SSR](#ssr)의 서버 스냅샷은 기본
  경로에만 적용돼요. 호스팅된 스코프는 클라이언트에서 시드되므로, 클라이언트 전용 devtools
  레이아웃에 쓰세요.
</Callout>
