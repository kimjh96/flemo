{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    "helpers:pinGitHubActionDigests"
  ],
  "timezone": "Asia/Seoul",
  "schedule": ["after 10pm on sunday", "before 7am on monday"],
  "rangeStrategy": "bump",
  "labels": ["dependencies"],
  "prHourlyLimit": 4,
  "prConcurrentLimit": 8,
  "packageRules": [
    {
      "description": "Auto-merge dev-dependency patch/minor bumps after CI passes.",
      "matchDepTypes": ["devDependencies"],
      "matchUpdateTypes": ["patch", "minor"],
      "automerge": true,
      "automergeType": "pr",
      "automergeStrategy": "squash"
    },
    {
      "description": "Group all fumadocs packages together. They version in lockstep.",
      "groupName": "fumadocs",
      "matchPackagePatterns": ["^fumadocs"]
    },
    {
      "description": "Group Vite + Vitest + plugins.",
      "groupName": "vite",
      "matchPackagePatterns": ["^vite$", "^vite-", "^@vitejs/", "^vitest$", "^@vitest/"]
    },
    {
      "description": "Group Next.js + its proxy/lint plugins.",
      "groupName": "next.js",
      "matchPackageNames": ["next", "eslint-config-next"]
    },
    {
      "description": "Group React + react-dom + their types.",
      "groupName": "react",
      "matchPackageNames": ["react", "react-dom", "@types/react", "@types/react-dom"]
    },
    {
      "description": "Group ESLint + typescript-eslint + related plugins.",
      "groupName": "eslint",
      "matchPackagePatterns": ["^eslint", "^@typescript-eslint/", "^@eslint/"]
    },
    {
      "description": "Group Changesets toolchain.",
      "groupName": "changesets",
      "matchPackagePatterns": ["^@changesets/"]
    },
    {
      "description": "Pin major bumps for major libraries. Surface them but don't auto-merge.",
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "labels": ["dependencies", "major"]
    },
    {
      "description": "Hold ESLint at v9. eslint-plugin-react calls the pre-v10 rule context API (react/display-name dies on contextOrFilename.getFilename), 7.37.5 is the latest published, and its own peer range stops at ^9.7. Lift this when that plugin ships ESLint 10 support. See #427.",
      "matchPackageNames": ["eslint", "@eslint/js"],
      "allowedVersions": "<10"
    },
    {
      "description": "Hold TypeScript at v6. vite-plugin-dts, through unplugin-dts, needs the JavaScript Compiler API that TypeScript 7 no longer ships, and both packages are already at their latest. The only workaround is carrying @typescript/typescript6 as a second TypeScript purely to generate the same .d.ts files. Lift this when unplugin-dts supports TypeScript 7 natively. See #422.",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7"
    }
  ],
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["after 10pm on the first day of the month"]
  },
  "vulnerabilityAlerts": {
    "labels": ["security"],
    "automerge": false
  }
}
