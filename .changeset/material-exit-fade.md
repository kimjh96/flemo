---
"@flemo/core": patch
---

Fade the material transition's outgoing screen as it slides up, so the previous screen dissolves instead of just nudging behind the incoming one. Swipe-back mirrors the same fade.
