---
"@flemo/core": patch
---

Keep the zoom preset on the box carriage: the transform carriage was tried as its default and rejected on device — a compositor flight scales type instead of re-typesetting it. The one-clock mode stays available as an explicit option.
