---
"@flemo/web": patch
---

Lower the detail header into place with a slide instead of a bare fade, and pair the act's name as a text morph on every bench case rather than only the container transform.
