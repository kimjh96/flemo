---
"@flemo/web": minor
---

Add the live shiflo viewer to the app-shell hero: a nested flemo Router slides the real shiflo App Store screenshots on the shared axis, with tap-to-advance and page dots. This dogfoods a nested-Router local region (in-memory history) running inside the root Router shell.
