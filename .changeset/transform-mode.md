---
"@flemo/core": minor
---

Add mode: "transform" to morph transitions and turn it on for the zoom preset: the container flight is staged at destination geometry inside a clipping window and travels wholly by compositor transforms — sampled inverse-scale keyframes hold the contents at natural size, nested pairs ride as transforms, and the flight, its ghost and its camera all resolve on one clock, which ends the Safari-only trembling of the pieces against each other.
