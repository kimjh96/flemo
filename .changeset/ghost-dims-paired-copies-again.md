---
"@flemo/core": patch
---

Dim paired copies inside the ghost again: the real pair flies from the captured pose, so the copy is a window onto it, and a painting copy stretched with the ghost's transform over the crisp re-typesetting original.
