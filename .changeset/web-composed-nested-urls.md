---
"@flemo/web": minor
---

Docs and playground sub-pages are now real composed URLs under `/docs` and `/playground` instead of fake `/doc` and `/stage` paths. They are deep-linkable and refresh-safe (no more 404s), and browser back/forward walks between them.
