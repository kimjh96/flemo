---
"@flemo/web": patch
---

Hold the returning row's unpaired venue and price lines back until the zoom pop's ghost has thinned, scoped to the row staged in the flight layer so the rows at rest never blink.
