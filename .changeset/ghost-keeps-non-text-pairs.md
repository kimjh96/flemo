---
"@flemo/core": patch
---

Keep a paired non-text descendant painting inside the ghost. The ghost sits on top of the arriving card, so the real flying element does not show through it, and dimming every paired copy left a hole the size of the pair on the flight's first frame: a full hero collapsed to a strip the instant a pop began. Only paired text still stops painting in the copy, since a re-typesetting run printed twice reads as a shadow trailing the text; everything else keeps its pixels, and the copy rides the ghost's own travel toward the destination box, tracking the real element closely enough for the cross-fade to read as one thing.
