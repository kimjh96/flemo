---
"@flemo/core": patch
---

Extend the proportional corner interpolation to per-corner radius lists, so a card whose image rounds only its top keeps each corner's own proportion through the flight.
