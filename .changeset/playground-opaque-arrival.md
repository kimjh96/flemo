---
"@flemo/web": patch
---

Stop the playground's detail screen showing the list through it, and pair the shared element between two layouts that share an arrangement. The `sheet` transition now settles a little scale at full opacity instead of fading the arriving page in over a still-legible one, and the listings are a grid so the poster and the name travel without crossing.
