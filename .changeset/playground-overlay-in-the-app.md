---
"@flemo/web": minor
---

Demonstrate `<Layer>` inside the ticket app instead of on a separate harness route: a booking sheet opened as a `useStep` sub-state that has to cover the tab bar, with a single toggle for whether it leaves its screen. Replaces the deleted layer suite with one written against the app's own layout.
