---
"@flemo/core": patch
---

Land a camera on the pose it rests at: the zoom that carries a screen now reaches its endpoint one frame before it ends and holds there, so the landing no longer releases a sliver of scale that moved everything on screen by its distance from the origin.
