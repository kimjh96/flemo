---
"@flemo/web": minor
---

Rebuild the playground around one app and one clock. Every `<Part>` is now generated per screen transition from a table that mirrors flemo's own preset timings, fixing a hand-over that finished at a quarter of a `cupertino` flight, and the page becomes a single stage with three cases instead of two copies of the landing hero.
