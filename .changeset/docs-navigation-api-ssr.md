---
"@flemo/web": patch
---

Docs: document the `skip` / `until` options in the Navigation options table and add a stack diagram for reaching past the top, add a `useScreen` return-shape reference to the API page, and reframe the server-side rendering section away from Next.js — flemo owns client-side history, so it doesn't compose with the Next.js App Router.
