---
"@flemo/web": minor
---

Rebuild the playground dev panel into categorized sections — transitions, shared bars (separate navigation/app bar toggles with a live presence read-out), screen chrome (status/system-nav bar size and tint, background, scroll), a navigation-distance demo for `{ skip }` / `{ until }`, performance, and a live inspector showing flemo's history stack, navigation status, and shared-bar registry in real time.
