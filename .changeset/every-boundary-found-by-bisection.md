---
"@flemo/core": patch
---

Find a type morph's face boundaries by bisecting the flight itself instead of aiming from the face's per-em ratios. An engine whose rounding drifts off the ratio arithmetic used to lose a stop, and a lost stop landed its whole step one frame before the landing, read on iOS as a meta line dropping a pixel at the end of every zoomed pop.
