---
"@flemo/core": patch
---

Interpolate a square pair's border-radius as a percentage of its box, so the roundness the eye reads eases over the whole flight instead of collapsing in the first frames when the box grows severalfold.
