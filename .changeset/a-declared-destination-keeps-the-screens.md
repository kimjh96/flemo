---
"@flemo/core": minor
---

Let a transition name where its drag carries the screens and still write a hook. Declaring `current` or `prev` keeps the screens on the scrub whatever hooks are written beside it, so a gesture that carries a morphing element about needs a hook only for the element and leaves the screens, which are the expensive half of a release, to flemo.
