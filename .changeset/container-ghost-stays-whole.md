---
"@flemo/core": patch
---

Keep a container's ghost on one transform: splitting its position onto the main thread while its violent scale stayed accelerated tore the copy against itself on WebKit, which was the zoom pop's trembling. An element's ghost still slides by left/top to hold its element's clock.
