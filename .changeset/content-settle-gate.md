---
"@flemo/core": minor
"@flemo/react": minor
---

Enter complete: a freshly-mounted entering screen whose requests are still in flight now waits (bounded) for its first content wave to land and settle before the motion starts, so a cold navigation slides in already filled instead of assembling mid-flight. Warm entries and pops pay nothing.
