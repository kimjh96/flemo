---
"@flemo/react": minor
---

Add `usePathname`, a public hook that returns the active pathname reactively. It lets chrome rendered outside a `<Screen>` (a header or sidebar) highlight the current route without reaching into the stores.
