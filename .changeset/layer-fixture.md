---
"@flemo/web": patch
---

Give the overlay layering case its own full-viewport playground route at `/playground/layer`, with a nested Router under an ancestor-owned bar, a sheet that renders identically inline and through `<Layer>`, and a registration line that breaks on sight when an overlay fails to travel with its screen.
