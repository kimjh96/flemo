---
"@flemo/web": minor
---

Rebuild the playground from the library author's own demos: one shared element that keeps its shape, one tab bar whose ride-or-hold behaviour is decided by the pair, and a transition selector that no screen knows about. Wires `@flemo/devtools` rather than reinventing a flight recorder.
