---
"@flemo/core": patch
---

Lay a `<Part>` inside a flying morph out at the width it rests at, rather than at every width the box passes through. A page of copy inside a growing card re-wrapped mid-flight and the content under it jumped a line up the screen, after the part had already been brought back to full opacity.
