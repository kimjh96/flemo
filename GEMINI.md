# Gemini CLI discovery adapter

This filename is a Gemini CLI convention, not a cross-agent standard. `AGENTS.md` is the canonical source of shared repository instructions; this adapter makes it discoverable without creating a second instruction set.

@./AGENTS.md
