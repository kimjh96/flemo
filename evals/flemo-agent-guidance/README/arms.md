# Why four arms

Documentation and skill intentionally overlap; comparing only docs against skill cannot identify either one's contribution. Every primary session receives one anonymous corpus:

1. declarations
2. declarations plus docs
3. declarations plus skill
4. declarations plus docs plus skill

The task prompt, starter, runtime packages, timeout, and evaluator stay identical. The agent cannot see the arm label.
